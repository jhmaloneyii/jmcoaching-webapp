<% provide(:title, "Edit Post: #{@post.title}") %>
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h1>Editing Post</h1>

      <%= render 'form', post: @post %>

      <%= link_to 'Show', @post %> |
      <%= link_to 'Back', admin_posts_path %>
    </div>
  </div>
</div>
